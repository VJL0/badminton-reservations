/** The big pill used for the primary action on every screen: shadcn Button + the display face. */
export const ctaClass =
  "h-14 w-full rounded-full px-8 font-display text-2xl font-extrabold uppercase tracking-[0.06em] lg:h-16 lg:text-[1.625rem]";

/** Section headings on the officer pages. */
export const headingClass = "font-display text-3xl font-extrabold uppercase tracking-display";

/** Small monospace captions and labels on the officer pages. */
export const metaClass = "font-mono text-xs uppercase tracking-meta text-muted-foreground";
